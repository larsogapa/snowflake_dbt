version: 2

sources:
  - name: TPCH
    description: base de datos de snowflake
    database: |
      {%- if target.name == "default" -%} SNOWFLAKE_SAMPLE_DATA
      {%- elif target.name == "dev" -%} SNOWFLAKE_SAMPLE_DATA
      {%- elif target.name == "qa" -%} SNOWFLAKE_SAMPLE_DATA
      {%- elif target.name == "prod" -%} SNOWFLAKE_SAMPLE_DATA
      {%- else -%} invalid_database
      {%- endif -%}
    schema: |
      {%- if target.name == "default" -%} TPCH_SF1
      {%- elif target.name == "dev" -%} TPCH_SF1
      {%- elif target.name == "qa" -%} TPCH_SF10
      {%- elif target.name == "prod" -%} TPCH_SF10
      {%- else -%} invalid_database
      {%- endif -%}
    tables:
      - name: CUSTOMER
        description: table de clientes
        meta: 
           owner: 'carlos'
      - name: LINEITEM
        description: tabla de base de datos
        meta:
          owner: 'Carlos'
      - name: NATION
        description: tabla de catalogo de naciones 
        meta:
          owner: 'Carlos'
      - name: ORDERS
        description: tabla de ordenes 
        meta:
          owner: 'Carlos'
      - name: PART
        description: tabla de Partes
      - name: PARTSUPP
        description: PARTSUPP
      - name: REGION
        description: Region
      - name: SUPPLIER
        description: Supplier



